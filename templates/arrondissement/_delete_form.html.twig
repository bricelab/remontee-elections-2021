<form method="post" action="{{ path('arrondissement_delete', {'id': arrondissement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ arrondissement.id) }}">
    <button class="btn">Delete</button>
</form>
